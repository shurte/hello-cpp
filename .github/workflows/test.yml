name: Main Challenge Test

on:
  push:
    branches: [  ]

jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
    - name: prepare
      run: |
        dir
        cd main_challenge/main_challenge_feffort_002
        cmake -S . -B build
        cmake --build build
